# Windows Paper B runtime repair — 11 September 2026

The shared Python 3.12 environment reproduced an Application Control import error in SciPy `_traversal`; Code Integrity events also identified `_ppoly`. Events 3077/3033/3118 attributed blocking to Smart App Control. The two installed binary hashes matched their distribution RECORD entries; corruption was not established.

Created the separate environment `E:\Master_Thesis\.venvs\paper-b-20260911` from the existing CPython 3.12 interpreter. Installed official PyPI wheels with the exact versions in `specs/B/requirements-windows.txt`. No Smart App Control policy, shared Python package or security setting was changed. The original shared SciPy 1.18.1 installation remains outside this repair; use the explicit project interpreter.

Both `scipy.interpolate._ppoly` and `scipy.sparse.csgraph._traversal` imported successfully. `Ridge().fit([[0],[1],[2]],[0,1,2]).predict([[1]])` returned `[1.]`. Imported versions: NumPy 2.4.6, SciPy 1.17.1, scikit-learn 1.9.0.

The initial test attempt encountered sandbox temporary-directory PermissionErrors, distinct from Application Control. The strict runner now uses project-local scratch files. The completed test command was run with approved elevated execution:

```powershell
& 'E:\Master_Thesis\.venvs\paper-b-20260911\Scripts\python.exe' tools/check_paper_b.py
```

Result: **66 tests, 93.060 seconds, zero failures/errors/skips**. Full output: [windows_20260911_tests.log](windows_20260911_tests.log). This checks existing components, including genuine synthetic ridge development/evaluation; it is not a connected raw-assay pipeline or AIU/cohort validation. The runner deliberately fails on skipped tests and empty discovery. Top-level discovery previously found zero tests because the subdirectories are not test packages.

Official documentation: [Python virtual environments](https://docs.python.org/3.12/library/venv.html), [SciPy installation](https://scipy.org/install/), [Microsoft Smart App Control](https://learn.microsoft.com/en-us/windows/apps/develop/smart-app-control/overview). Successful imports in this pinned environment are observed evidence, not a promise that future unpinned builds will be accepted by Windows.
