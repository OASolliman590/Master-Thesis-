# M1 final orchestrator disposition

6 September 2026, Africa/Cairo; review run 5 September20:57–21:06 UTC. **Release: reviewed WIP design milestone. Scientific production approval: withheld. B-F1 format-only ticket: released within the limits below.** The programme goal remains active and all four papers remain in scope.

## Review evidence and limits

Opus `claude-opus-4-6-thinking`, Antigravity1.1.27, completed the bounded review. The relay reports `readOnlyViolation=false`; listed touched paths were pre-existing M1 changes, not reviewer edits. The raw review was written to Antigravity's own artifact store and copied here without changing its scientific content. Run provenance and the pre-review file hashes are adjacent. The first oversized inline brief was rejected before dispatch; the completed run used a compact brief with file access.

Opus used five internal research subagents and reports a mixture of direct and mediated inspection. Their model identities are not independently verified. Its “38 directly inspected” tally includes some entries labelled mediated and is not treated as an exact independent file count. Several A engineering/preregistration/ticket bodies were outside its direct coverage; the earlier separate A review covered those contracts and its corrections, and the orchestrator inspected the integrated design. This is bounded review of all four packages, not line-by-line certification or external scientific replication.

The orchestrator independently checked 59 JSON files, syntax of 15 research scripts, all linked spec-local paths, 32 LINCS cached-artifact hashes and 34 B/D cached-artifact hashes at the pre-release stage. These checks establish structural/provenance consistency only. No biological score/model, C scorer runtime or D inference ran. The complete B expression audit was an actual AIU source/feature check, not a predicted endpoint result.

Release-stage Git warnings exposed automatic line-ending normalization, which can alter byte-level provenance hashes between Windows and AIU. Repository-local `.gitattributes` now preserves exact bytes; staging is refreshed and compared byte-for-byte with the inspected files. This portability correction changes no scientific decision and no global Git setting. Historical M0 commits remain untouched.

## Findings and dispositions

| Opus finding | Orchestrator judgment and action |
|---|---|
| F1 A primary emphasis | Valid material decision. Keep A-P1 and A-P2 as competing proposals; the final scientific contract must select one before confirmatory scoring. The 49/89 records are **eligibility ceilings**, not admitted samples, and A-P1 is not yet proven executable on two cohorts. A-P2 needs an actual originating-study/split audit. No automatic narrowing is accepted. |
| F2 B external N/precision | Valid gate; reject the unsupported assertion that its CI necessarily “will be wide.” Width depends on paired errors, heterogeneity and target effect. Candidate grade/age/WGS purity cover73 codes;70 additionally have histologic cellularity. Neither is the final primary N. Assess precision after linkage/QC, using declared development assumptions and sensitivity scenarios. |
| F3 B program citation | Valid refinement. Do not cite the suggested Senbabaoglu source as supporting this exact set without checking it. The set is a design choice with individually supported biological roles, not a published validated eight-gene clinical signature. A bounded primary-source/annotation audit is underway outside M1. |
| F4 C sparse/project-confounded anchor | Accepted and already explicit. A descriptive conditional score is valid; a between-drug causal superiority claim is not.10micromolar remains a separately labelled sensitivity, not an extra primary selected for stronger signal; tazemetostat still has one collapsed signature there. HIQ selection is partly functional and cannot cure replication or batch confounding. |
| F5 C novelty requires changed rankings | Reject “only if rankings change” and automatic combination after a null result. Added knowledge depends on the question, independent validation, precision and explanation, not favorable change. A rigorous finding that methylation adds little, or that inferred/short-duration evidence misleads, can be informative. A familiar unvalidated ranked list remains weak. Standalone/combine judgment is substantive and reviewable, not a significance gate. |
| F6 D feasibility | Preserve conditional design; no verified qualifying test exists today. Reject “vocabulary lacks drugs entirely”: the audit is a literal alias check, with structural identity unresolved. No located dataset is not proof of nonexistence or no possible route. Deferral and the earlier15-hour cap remain proposals requiring a programme decision; they do not cancel D. |
| F7 D fallback | Accepted invariant already in the tickets. Unsupported labels must reject before prediction; control-like fallback can masquerade as a supported result and distort accuracy. It is not always a false-positive drug effect. No D code is released now. |
| F8 B platform comparability | Valid threat, already acknowledged rather than asserted solved. Add an independent calibration/measurement-comparability design before freeze where available. Do not inspect final CPC outcome distributions to choose the transformation, or introduce TCGA/CPC overlap into “external” validation. Cohort distribution differences alone cannot isolate platform bias. Without appropriate calibration evidence, retain the threat explicitly and reassess the proposed transport estimand. |
| F9 A prostate claims | Accepted. Molecular distributions and regimen-specific retrospective discrimination are distinct from validated prostate ICI treatment benefit. Guan matrix/label linkage remains unsecured, not proven permanently unavailable. |
| F10 C scoring runtime | Accepted gate; reject synthetic-oracle-only “parity” and unverified Octave compatibility. A Python port needs comparison with an actually executed/independently verified pinned reference, alongside hand-worked invariants. Docker presence is not validated daemon/image/tool compatibility. C coding waits for its sequential turn and specific release; it does not start in parallel with B production. |
| F11 B annotation/masks | Accepted factual gate. A valid source and version must be selected; the review's Chen-mask reference is a suggestion, not an accepted mandatory mask. Current annotation research is bounded and does not compute promoter scores. |
| F12 A secondary family | Accepted. Freeze exact score implementations, tested contrasts and denominator before inferential execution; framework alone is not a finished secondary panel. |

The raw review calls its list “6 blockers,6 refinements” in its final message, but the detailed labels differ. We use the substantive item dispositions above, not that unverified count. Its assertion that a methylation-biology primary necessarily requires causal evidence is also rejected: a clearly framed replicated association study is possible without causality. Whether that better serves B is a material alternative, not eliminated by reviewer authority.

## B-F1 release

All four design packages have now received bounded independent scientific/spec review, with the above corrections retained. Under the user's authorization for sequential implementation and routine engineering choices, release **only B-F1**: local uncompressed-TSV format inspection at a subprocess boundary, Python3.11 standard library, four already audited fixtures, checksummed count/schema summaries and adversarial parser tests. This slice is invariant to the unresolved biological primary choices.

Owned paths: `tools/b_formats/`, `tests/b_formats/`, `docs/validation/B_formats/`. No root/shared configuration, raw-source, other-paper or analysis-spec edits by the implementer. No network/download, environment installation, scores, patient associations, normalization, gene/probe selection or model fitting. Real fixtures stay outside Git; only synthetic fixtures, code, public source manifests and permitted aggregate format summaries may be committed. Full hashed input bytes are checked; declared completeness is not inferred from TSV syntax alone.

Before dispatch: publish this M1 WIP milestone, then provide the exact reviewed B-F1 contract and source hashes to the verified Spark route. After dispatch: inspect every diff, run meaningful local and AIU real-format checks, obtain Opus code review and resolve findings, then Astra final review/commit/push/Notion. A passed B-F1 ticket is **format tooling implemented**, never “Paper B implemented” or biological gates passed. Other scientific production remains closed.

## Programme state

No primary proposal, narrowed manuscript or D deferral becomes a user decision through this review. Next material discussion concerns A's primary emphasis and B's predictive versus replicated-association emphasis, informed by bounded data feasibility; C's anchor and manuscript scope remain reviewable. All four conditional packages continue to require evidence resolution. Future source findings obtained while Opus reviewed were kept outside the snapshot and will be integrated with their own explicit provenance/review.
