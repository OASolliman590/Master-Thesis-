# B-F1 Spec recheck

Reviewed the staged candidate against M1 `43efda19bff062a2e9e140fadd84ab32c88cee71`, including all 17 owned files, the verification driver and retained four-format receipts. No unstaged differences existed in these paths. No production files edited or tests rerun.

**The six previously reported findings are resolved in the reviewed code/documentation.** Hashing, byte count and parsing share one immutable byte snapshot. Completed output publication refuses competing destinations using Windows rename or POSIX hard linking, with explicit failure semantics and no overwrite fallback. STAR recognizes four summary IDs and only permits structural blanks in normalized fields; CPC methylation now rejects header-only input. The expression prefix's parent is correctly recorded as 256,000 bytes with SHA256 `ccb85485c521a3e02d8fd0d9edb5e15f096d9726a486da67f2e24e24b4b7099c`. README commands cover Linux, Windows temporary roots, real-fixture availability and pending AIU validation.

**One small acceptance-test correction remains [P2].** Requirement: “Construct a genuinely swapped detection header with a complete-width row” (`planning/delegation_runs/B_F1_spark_delta.txt`, item 2). In `tests/b_formats/test_bf1_cli.py:330–331`, the header now genuinely swaps the labels, but the row still supplies only three assay values for four assay columns. Add the fourth value so this case isolates header pairing rather than also containing a width defect. The current specific error-message assertion does detect the intended header rejection; this is an incomplete requested fixture correction, not evidence that the parser accepts swapped columns. The separate wrong-width regression should remain.

Independently recomputed all five code/contract hashes and four summary hashes in `local_20260906/run.json`; all match current files. Each summary matches its retained expected fields and manifest input SHA. Receipts document actual local Python 3.12 execution, successful four-format inspections and no biological validation. The driver uses fresh output directories, bounded subprocess calls, exact commands/status and aggregate-only receipts. Root reports 19 passing tests and five pre-fix regression failures; I did not rerun them. AIU/Python 3.11 and POSIX publication behaviour remain unexecuted here, explicitly pending rather than certified.

## Follow-up, 6 September 2026

Read-only confirmation: `tests/b_formats/test_bf1_cli.py:331` now contains the probe identifier plus four assay values. This closes the remaining complete-width fixture finding. The parent reports the affected test passed in 0.570 seconds; I did not execute it.

Also inspected `docs/reviews/B_F1_ASTRA_DISPOSITION.md` and `docs/execution/pending_validation.json`. No materially false completion claim identified: both retain B-F1 as open/reviewed WIP, distinguish the 19-test run from the subsequent one-test correction, and explicitly leave AIU/Python 3.11/POSIX execution and final Opus recheck pending. The disposition correctly rejects the quota-limited exit-0 acknowledgement as completed review and limits publication to a WIP backup. This follow-up does not independently verify provider logs, remote state or another reviewer's actions. No tests, network calls or canonical repository edits were performed.
