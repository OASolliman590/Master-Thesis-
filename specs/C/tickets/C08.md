# C08 — Materialize compound mechanism and exposure annotation

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering-research.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C01, C03; lawful source access; selected primary humanPK records.

## Bounded task

Map exact ChEMBL identity/mechanism/activity source records and retain DrugBank provider paused status until lawful object available. For each protocol drug capture primary PK document, regimen, formulation, route, total/free units, bindingmethod and time.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/context/pharmacology.py; configs/paper_c/pk_sources.json; tests/paper_c/test_pk_units.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Each claim has originaldocumentlocator; unitconversion fixture checked; free plasma calculation labelledassumption; unknown medium/tumour exposure remainsnull. MissingDrugBank annotates only its layer.

## Exclusions

No dosing recommendations, invented clinical Cmax, opaque evidence weights or scraping around access controls.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
