# C11 — Gate and execute conditional sensitivity analyses

Status: **proposed / blocked; not ready-for-agent**. Kind: conditional-engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C03, C05, C06; individual secondary estimands and families frozen.

## Bounded task

Implement only reviewed query-size/landmark/dose/HIQ and expression-only comparators. A query-null module requires an approved matching rule, seed anddrawcount; class inference requires curated compound design rank/projectoverlap andexplicit model formula.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/secondary/; configs/paper_c/secondary.json; tests/paper_c/test_secondary_gates.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Primary outputs unchanged by secondary settings; scope/status table for each conditional module; unsupported class design emit nonestimable with reason. Query-null p labelled specificity, BH family explicit, no biological effect claim.

## Exclusions

No automatic search of favorable thresholds, doses, classes or unreviewed high capacity model.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
