# C12 — Generate figures and reproduce the complete evidence bundle

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering-and-review.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C06 and all required claim-specific modules resolved or scope amended.

## Bounded task

Generate four figures from versioned source tables and declared captions; implement restart/hash verification and export exact configs, manifests, exclusions, mappings, environment and pending checks. Recreate one approved complete workflow from clean cache per data access policy.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/reporting/; tests/paper_c/test_report_provenance.py; docs/validation/C_reproduction.md`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Every plotted value traces to score/context table; conflicts/missing arms visible; interrupted job never reported passed. Opus review plus Astra disposition checks concrete diff and evidence. Orchestrator alone publishes reviewed milestone/GitHub issue/Notion status.

## Exclusions

No fabricated panels, unsupported biological CI, automatic commit/push or clinical claim inflation.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
