# C04 — Acquire and verify a bounded Level5 primary slice

Status: **proposed / blocked; not ready-for-agent**. Kind: data-engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C00, C01, C03; actual matrix URL/storage route approved.

## Bounded task

Inspect official matrix metadata before transfer. Acquire only a reviewed slice containing the exact common primary signatures and full frozen feature universe through a verified compatible access route. Record originalURL, byte budgets, matrix row/column IDs, source hash/version and retained content hash.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/io/lincs.py; tests/paper_c/test_lincs_slice.py; configs/paper_c/lincs_slice_manifest.json`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

All expected primary IDs and feature tiers match the complete metadata; wrong order, missing columns and stale versions reject; bounded extraction reports completeEOF or verified range coverage. Local and AIU re-read agree.

## Exclusions

No guessed object filenames, hidden full matrix download, gene-subset reranking or exposure interpolation.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
