# C00 — Freeze scientific gates and query interface

Status: **proposed / blocked; not ready-for-agent**. Kind: scientific-review.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: All four packages reviewed; primary/weighting/QC decisions unresolved.

## Bounded task

Resolve C-D02 through C-D06 against metadata before effect inspection; specify B discovery bidirectional handoff and expression-only comparator. Inventory prior exposure and identify actual independent validation candidates. This ticket produces a decision record, not an analysis.

## Owned files

Proposed implementation paths: `specs/C/PREREGISTRATION.md; specs/C/decision-disposition.json (new only after review)`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

A review disposition names each approved/conditional/rejected item, evidence, timestamp and reviewer; no source silently removed; unresolved fields remain blocked. Publish parent issue only after scientific review.

## Exclusions

No molecular signature creation, drug scoring, endpoint changes by implementer or protocol revisions.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
