# C02 — Resolve isolated scorer runtime and rank conventions

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering-research.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C00; no production scoring until parity passes.

## Bounded task

Select an available isolated official MATLAB/compiled route; verify exact executable/container/source identities. Run small synthetic fixtures to establish upstream rank-tie, ES-tie, zero-boundary and rescale=false semantics. Freeze package/build hashes and recreate environment.

## Owned files

Proposed implementation paths: `env/paper_c/; tests/paper_c/reference_fixtures/; docs/validation/C_runtime.md`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Pinned reference source 8f0e7bc4c38efbdf3f96f5ac72ee26792907a99c; actual fixture inputs/outputs, execution logs and environment digests; no mutable tag masquerades as lock. If runtime unavailable, retain blocked status rather than claim parity.

## Exclusions

No shared AIU environment modifications, unreviewed substitute scorer or biological matrices.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
