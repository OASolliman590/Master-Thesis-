# C07 — Materialize baseline and sensitivity context

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C01, C03; selected CCLE/DepMap/PRISM/GDSC objects and terms approved.

## Bounded task

For the three protocolcompounds and audited prostate models, implement measured model/profile and sensitivity joins with assay scale, units, screen, dose, QC and origin. Generalize adapter schema to a later curatedpanel without claiming unqueried coverage.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/context/models.py; src/thesis/paper_c/context/sensitivity.py; tests/paper_c/test_model_sensitivity_context.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Produce measured-versus-catalogued grid; absent methylation/nonmissingviability unknowns explicit; PRISM primary/secondary and GDSC scale preserved; mirrors share origin; dependency annotated without claiming immune regulation.

## Exclusions

No missing-value imputation as effect, cross-assay pooling or expansion into pan-cancer fitness modelling.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
