# C01 — Implement metadata schemas and admission boundary

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C00 plus reviewed schema contract.

## Bounded task

Implement ENGINEERING table contracts and source-status vocabulary. Validate string IDs, units, explicit nulls, source hashes and originating studies; materialize a coverage table whose dictionary-only entries remain catalogued.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/contracts/; tests/paper_c/test_contract_admission.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Fixtures reject ambiguous identity, missing hash for a purported usable object, unknown units and measured-state claims based on catalogue products; real audit metadata parses with record/field provenance.

## Exclusions

No matrix acquisition, scoring, model fitting or general rewrite of other paper contracts.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
