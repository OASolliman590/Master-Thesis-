# C03 — Curate panel identity and instance provenance

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C00, C01.

## Bounded task

Create structure-aware BRD/ChEMBL/model joins; retain original MOA and explicit corrections. Resolve BIX-01294, azacitidine duplicates, E-7438 and EI1 with citations. Build instance-overlap groups from complete 2020 retained metadata and known 2017 source.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/identity/; tests/paper_c/test_identity_overlap.py; configs/paper_c/curated_panel.json`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Exact three-drug anchor counts reproduce; raw BRD count distinguished from unique molecules; current 93,958 prostate chemical rows have no repeated instances within2020;2017 overlap cannot be labelled independent. MissingPhaseI/structure/biological-unit data remain unknown.

## Exclusions

No inferred mechanism from name alone; no class fitting or selection by drug effects.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
