# C06 — Implement raw WTCS and descriptive primary output

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C01-C05 passed; C primary material decisions frozen.

## Bounded task

Implement or call the reviewed reference route. Compare numerical output on exact fixtures, then score the approved real slice and compute negative equal-signature mean separately for each protocol drug at PC3/24 h / 1.11111 uM. Export component scores, project/QC/instance counts and no biological CI.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/scoring/; src/thesis/paper_c/primary.py; tests/paper_c/test_wtcs_parity.py; tests/paper_c/test_primary_mean.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Parity meets reviewed tolerance; ES zero/tie/sign/rank fixtures pass; direction oracle passes; invalid query never returns successful zero. Real matrix smoke completes without biological direction threshold; output SHA and run provenance recorded.

## Exclusions

No NCS/tau relabelling, class superiority test, extra dose choice or pseudoreplicate bootstrap.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
