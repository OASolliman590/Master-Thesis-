# C05 — Validate the B discovery query handoff

Status: **proposed / blocked; not ready-for-agent**. Kind: engineering.

Parent: Paper C specification, GitHub issue URL pending. All-four-paper scientific review is a global production gate. Local mirrors are not authorization to run analyses.

Dependencies: C00, C01; B signed discovery handoff and expression-only comparator frozen.

## Bounded task

Admit the two frozen query bundles with signed disease axis, gene/probe/build selection provenance and hashes; map to the fixed L1000 feature universe without using C scores or B validation outcomes.

## Owned files

Proposed implementation paths: `src/thesis/paper_c/query/; tests/paper_c/test_query_handoff.py`. These paths are not created by this ticket. Do not edit files outside this scope; request an explicit ownership amendment for a necessary shared change.

## Acceptance evidence

Both UP/DOWN are nonempty anddisjoint; absent genes retained incoverage; no fixed eight-gene substitution; perturbing heldout outcomes cannot alter handoff. One-direction-only input blocks primary and requests a scientific amendment.

## Exclusions

No query gene invention, canonical-recovery pass threshold or drug-result-driven gene selection.

## Handoff

Read AGENTS.md, CONTEXT.md, specs/C and exact source documentation before work. Keep original URLs, field mappings, inspected revisions and result logs. Use the configured delegation lane only after dependencies resolve; return a concrete diff, tests actually run, pending checks and scientific limitations. Never commit/push or alter shared environments. AIU is primary; on VPN loss record pending_validation and inspect the prior remote job before resuming. Orchestrator owns review, issue synchronization and publication.
