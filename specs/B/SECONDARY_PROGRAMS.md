# Paper B secondary programs

Status, 7 September 2026: integrated conditional specification. The user approved one primary program, a small prespecified secondary panel and exploratory remaining sets. The source review recommends the candidates below; exact measurements and statistical inference are not yet frozen. This document does not release real-cohort scoring or change B-P1.

## Primary boundary

Keep the single primary external Delta_R2 for the primary antigen-presentation program, developed in TCGA and evaluated without refitting in CPC-GENE. Secondary analyses cannot replace this endpoint, select its genes, or block its implementation merely because a secondary program is not reproducible. Primary membership and measurement gates remain those in ANALYSIS.md.

## Candidate secondary analyses

| Candidate | Scientific question | Required measurement contract | Failure disposition |
|---|---|---|---|
| Ayers T-cell-inflamed GEP-18 | Does the frozen primary promoter feature program also add prediction of a broader bulk inflammatory/adaptive-resistance expression state? | Recover original coefficients, signed directions including CD276, normalization and defensible TCGA/CPC platform transport. Verify every member and deterministic missing/duplicate rules. | If the published score cannot be reproduced, defer confirmatory use. Any membership-only adaptation requires a distinct name and exploratory status. |
| Official HALLMARK_INTERFERON_GAMMA_RESPONSE | Does the same promoter feature program add prediction of a broader IFNG-response expression state? | Freeze an exact official membership release, actual single-sample scoring algorithm/version/parameters, common measurement universe and coverage rules. Do not use the saved 10-gene mini as the official set. | Defer if coverage or transport cannot support the frozen definition. Do not select a favorable substitute using external outcomes. |

Each outcome requires separately fitted baseline and extended models, with identical eligible patients within that comparison and all fitting/tuning in TCGA. Predictor expansion is not implied by adding an outcome. Record overlap with the final primary program; any overlap-excluded sensitivity is prespecified and descriptive. Zero overlap does not establish biological independence or a different cell of origin.

## Inference contract still required

The source handoff proposes Holm adjustment across two secondary tests. This is not yet an executable inferential procedure: specify valid per-comparison null hypotheses and p-value construction consistent with frozen-model external Delta_R2 before adopting that proposal. Do not turn percentile-bootstrap intervals into invented p-values. Report paired effect estimates and intervals; no confirmatory significance claim is released by this document. Exploratory tests need their own declared family and method.

## Exploratory collection

Keep the remaining recovered GMT panels available as documented candidates. Custom epigenetic composites mix regulators, targets and cell-type markers and lack a recovered common scoring direction. Canonicalize aliases, remove duplicate biological loci with an explicit rule, and verify original sources before scoring. The historical responder lists are separate artifacts and are not replacements for these gene-set collections.

## Acceptance before secondary execution

For each admitted program retain source/version/hash, canonical membership, signs/weights, scoring specification, annotation and finite-value coverage, missing-data rules, predictor/baseline definitions, patient eligibility, uncertainty and multiplicity methods, and an external-outcome access lock. Explicitly mark any deferred program. Do not repeatedly expand the panel to obtain favorable results.

Evidence: [source-and-coverage handoff](../../docs/research/B_SECONDARY_GENE_SET_PANEL.md), [machine-readable recommendation](../../docs/research/B_secondary_gene_set_panel/recommendation.json), [primary analysis](ANALYSIS.md).
